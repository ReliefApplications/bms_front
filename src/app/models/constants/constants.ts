export class Constants {

//
// ─── REGEX ──────────────────────────────────────────────────────────────────────
//
    static readonly REGEX_PASSWORD = new RegExp(/^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.{8,})/);

}

export interface ExportInterface {
    /**
     * @property content The content of the data to download
     */
    content: string;

    /**
     * @property filename The name of the file to download
     */
    filename: string;
}
